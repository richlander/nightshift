# Beta

The `beta` term is defined in the [stacktest contract](./contract.md).

The beta slot is the second variant slot: a leaf order stacked on the contract
commit, built concurrently with — and disjoint from — the alpha slot.
